package koko.model;

/**
 * The learning modes that maintain independent progress for a vocabulary card.
 */
public enum Mode {
    /** Recognition using a flashcard. */
    FLASHCARD,

    /** Production by typing the Hiragana answer. */
    TYPING
}
